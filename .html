<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>⚠️ Your Team Might Destroy Everyone After This</title>
    <link href="https://fonts.googleapis.com/css?family=Poppins:400,600,700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #22223B;
            --secondary: #4A4E69;
            --accent: #F2E94E;
            --highlight: #F2E94E;
            --text: #22223B;
            --muted: #9A8C98;
            --card: #F8F8F8;
            --white: #fff;
        }
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
        }
        body {
            background: var(--white);
            color: var(--text);
            min-height: 100vh;
            overflow-x: hidden;
        }
        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 40px 24px 0 24px;
            text-align: center;
        }
        .headline {
            font-size: 2.3rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 32px;
            color: var(--primary);
            letter-spacing: 1px;
            transition: text-shadow 0.3s;
            cursor: pointer;
            text-shadow: 0 2px 12px rgba(242,233,78,0.08);
            position: relative;
        }
        .headline .warning-icon {
            display: inline-block;
            animation: popin 0.7s cubic-bezier(.77,0,.18,1) infinite alternate;
            vertical-align: middle;
        }
        @keyframes popin {
            0% { transform: scale(1) rotate(0deg); }
            50% { transform: scale(1.25) rotate(-10deg); }
            100% { transform: scale(1) rotate(0deg); }
        }
        .headline span.underline {
            text-decoration: underline;
            text-decoration-thickness: 4px;
            text-underline-offset: 6px;
            color: var(--accent);
            transition: color 0.2s;
            background-image: none;
            display: inline-block;
        }
        .headline:hover span.underline {
            color: #e53935;
            animation: shake 0.4s;
        }
        @keyframes shake {
            0% { transform: translateX(0); }
            20% { transform: translateX(-4px); }
            40% { transform: translateX(4px); }
            60% { transform: translateX(-4px); }
            80% { transform: translateX(4px); }
            100% { transform: translateX(0); }
        }
        .headline:hover {
            text-shadow: 0 4px 24px rgba(242,233,78,0.15);
        }
        .video-wrapper {
            position: relative;
            padding-bottom: 56.25%;
            height: 0;
            overflow: hidden;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(74,78,105,0.08);
            margin-bottom: 32px;
            background: var(--card);
            animation: fadeIn 1.2s cubic-bezier(.77,0,.18,1) 0.4s both;
            max-width: 1000px;
            margin-left: auto;
            margin-right: auto;
        }
        .video-wrapper iframe {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            border: none;
            border-radius: 18px;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        .form-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 18px;
            animation: fadeInUp 1.2s cubic-bezier(.77,0,.18,1) 0.7s both;
            width: 100%;
            max-width: 1000px;
            margin-left: auto;
            margin-right: auto;
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(40px); }
            to { opacity: 1; transform: translateY(0); }
        }
        form {
            background: var(--white);
            padding: 24px 18px 18px 18px;
            border-radius: 14px;
            box-shadow: 0 2px 12px rgba(74,78,105,0.07);
            display: flex;
            flex-direction: column;
            gap: 14px;
            width: 100%;
            max-width: 1000px;
            margin: 0 auto;
        }
        input[type="text"], input[type="email"], input[type="tel"] {
            padding: 12px 16px;
            border-radius: 8px;
            border: 1px solid var(--muted);
            background: var(--card);
            color: var(--text);
            font-size: 1rem;
            outline: none;
            transition: border 0.2s;
            text-align: center;
        }
        input[type="text"]:focus, input[type="email"]:focus, input[type="tel"]:focus {
            border: 1.5px solid var(--accent);
        }
        .cta-btn {
            margin-top: 8px;
            background: var(--accent);
            color: var(--primary);
            font-size: 1.15rem;
            font-weight: 700;
            border: none;
            border-radius: 10px;
            padding: 14px 0;
            cursor: pointer;
            box-shadow: 0 2px 12px rgba(242,233,78,0.13);
            transition: background 0.3s, color 0.3s, transform 0.2s, box-shadow 0.2s;
            width: 100%;
        }
        .cta-btn:hover, .cta-btn:focus {
            background: var(--primary);
            color: var(--accent);
            transform: translateY(-2px) scale(1.04);
            box-shadow: 0 6px 24px rgba(74,78,105,0.18);
        }
        .countdown {
            position: static;
            left: unset;
            bottom: unset;
            width: auto;
            z-index: unset;
            margin: 18px auto 0 auto;
            text-align: center;
            font-size: 1.2rem;
            color: #fff;
            font-weight: 700;
            background: #e53935;
            border-radius: 0;
            padding: 18px 0 18px 0;
            box-shadow: 0 2px 12px rgba(229,57,53,0.12);
            letter-spacing: 1px;
            font-family: 'Poppins', sans-serif;
            font-size: 1.25rem;
            display: block;
            pointer-events: auto;
            max-width: 100vw;
        }
        .countdown span {
            font-size: 1.3em;
            font-weight: 700;
            letter-spacing: 1px;
        }
        .highlighted-text {
            margin: 22px auto 32px auto;
            text-align: center;
            font-size: 1.15rem;
            color: var(--primary);
            background: linear-gradient(90deg, var(--accent) 60%, var(--white) 100%);
            border-radius: 14px;
            padding: 22px 24px;
            font-weight: 700;
            max-width: 700px;
            box-shadow: 0 2px 18px rgba(242,233,78,0.13);
            border: 2px solid var(--accent);
            letter-spacing: 0.5px;
        }
        .dynamic-section, .dynamic-section + * {
            display: none !important;
        }
        .dynamic-section h2 {
            text-align: center;
        }
        .dynamic-steps {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            align-items: center;
            text-align: center;
            gap: 24px;
            width: 100%;
        }
        .step-card {
            background: var(--white);
            color: var(--primary);
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(74,78,105,0.08);
            padding: 24px 18px 18px 18px;
            width: 220px;
            min-height: 180px;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            animation: floatCard 2.5s ease-in-out infinite alternate;
            transition: transform 0.25s cubic-bezier(.77,0,.18,1), box-shadow 0.25s, background 0.25s;
            perspective: none;
            backface-visibility: visible;
        }
        .step-card:hover {
            transform: scale(1.07);
            box-shadow: 0 8px 32px rgba(74,78,105,0.18);
            background: #f9fbe7;
            z-index: 2;
        }
        .step-card img {
            width: 56px;
            height: 56px;
            margin-bottom: 12px;
            border-radius: 50%;
            box-shadow: 0 2px 12px rgba(242,233,78,0.18);
            animation: pulseImg 2s infinite alternate;
        }
        @keyframes pulseImg {
            0% { box-shadow: 0 2px 12px rgba(242,233,78,0.18); }
            100% { box-shadow: 0 8px 32px rgba(242,233,78,0.28); }
        }
        .step-title {
            font-size: 1.15rem;
            font-weight: 600;
            margin-bottom: 8px;
            color: var(--primary);
            text-align: center;
        }
        .step-desc {
            font-size: 1rem;
            text-align: center;
            color: var(--secondary);
        }
        section, .testimonials {
            margin: 0 auto 38px auto;
            max-width: 1000px;
            padding: 0 18px;
            border-radius: 18px;
            background: var(--card);
            box-shadow: 0 2px 12px rgba(74,78,105,0.07);
            text-align: center;
        }
        section h2, .testimonials h2 {
            transition: color 0.25s, background 0.25s, box-shadow 0.25s, transform 0.25s;
            display: inline-block;
            background: transparent;
            border-radius: 12px;
            padding: 6px 18px;
        }
        section h2:hover, .testimonials h2:hover {
            color: var(--accent) !important;
            background: #111;
            box-shadow: 0 4px 24px rgba(0,0,0,0.18);
            transform: scale(1.10) translateY(-4px);
            cursor: pointer;
        }
        section ul, section ol {
            margin-left: 18px;
            margin-bottom: 12px;
            text-align: center;
            display: inline-block;
        }
        section li {
            margin-bottom: 8px;
            font-size: 1.05rem;
            text-align: center;
        }
        .testimonials {
            padding: 24px 8px 32px 8px;
            position: relative;
            overflow: hidden;
            animation: fadeIn 1.2s cubic-bezier(.77,0,.18,1) 1.3s both;
        }
        .testimonial-slider {
            display: flex;
            transition: transform 0.7s cubic-bezier(.77,0,.18,1);
            will-change: transform;
        }
        .testimonial {
            min-width: 100%;
            flex-shrink: 0;
            padding: 0 18px;
            text-align: center;
            color: var(--primary);
            font-size: 1.1rem;
            font-weight: 500;
            opacity: 0.92;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .testimonial img {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 10px;
            border: 2px solid var(--accent);
        }
        .testimonial-author {
            margin-top: 8px;
            font-size: 1rem;
            color: var(--secondary);
            font-weight: 600;
        }
        .testimonial-controls {
            display: flex;
            justify-content: center;
            gap: 12px;
            margin-top: 18px;
        }
        .testimonial-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: var(--accent);
            opacity: 0.4;
            border: none;
            cursor: pointer;
            transition: opacity 0.2s, background 0.2s;
        }
        .testimonial-dot.active {
            opacity: 1;
            background: var(--primary);
        }
        @media (max-width: 900px) {
            .dynamic-steps { flex-direction: column; align-items: center; }
            .step-card { width: 90vw; min-width: 0; }
        }
        @media (max-width: 600px) {
            .container, section, .testimonials {
                max-width: 98vw;
                padding: 0 2vw;
            }
            .headline {
                font-size: 1.5rem;
            }
            form {
                padding: 12px 4px 8px 4px;
            }
            .highlighted-text {
                font-size: 1rem;
                padding: 10px 6px;
            }
            .dynamic-section { padding: 18px 4px 12px 4px; }
        }
        /* Remove list styles for clean, centered text blocks */
        section#what-i-do ul,
        section#how-to-start ol {
            list-style: none;
            padding-left: 0;
            margin-left: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        section#what-i-do ul li,
        section#how-to-start ol li {
            text-align: center;
            font-size: 1.08rem;
            margin-bottom: 10px;
            position: relative;
            padding-left: 0;
        }
        .final-cta-headline {
            text-align: center;
            font-size: 2rem;
            color: var(--primary);
            margin-bottom: 18px;
            font-weight: 700;
        }
        .final-cta-btn {
            display: block;
            margin: 0 auto 18px auto;
            background: var(--accent);
            color: var(--primary);
            font-size: 1.2rem;
            font-weight: 700;
            border: none;
            border-radius: 12px;
            padding: 16px 48px;
            cursor: pointer;
            box-shadow: 0 2px 12px rgba(242,233,78,0.13);
            transition: background 0.3s, color 0.3s, transform 0.2s, box-shadow 0.2s;
        }
        .final-cta-btn:hover, .final-cta-btn:focus {
            background: #111;
            color: var(--accent);
            transform: scale(1.06);
            box-shadow: 0 6px 24px rgba(74,78,105,0.18);
        }
        .final-timer {
            text-align: center;
            font-size: 1.5rem;
            color: #e53935;
            font-weight: 700;
            background: #fff3e0;
            border-radius: 8px;
            padding: 10px 0;
            width: 120px;
            margin: 0 auto 24px auto;
            box-shadow: 0 2px 12px rgba(229,57,53,0.08);
            letter-spacing: 2px;
        }
        .star-avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            border: 2px solid #fff;
            object-fit: cover;
            margin-left: -12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.07);
            background: #eee;
        }
        .star-avatar:first-child { margin-left: 0; }
        .star-avatar-small {
            width: 24px;
            height: 24px;
            margin-left: -8px;
        }
        .star-avatar-small:first-child { margin-left: 0; }
        .cool-bg-shape {
            position: fixed;
            z-index: 0;
            pointer-events: none;
            opacity: 0.13;
            animation: floatShape 12s linear infinite;
        }
        .cool-bg-shape.shape1 {
            left: 5vw; top: 12vh; width: 90px; height: 90px; background: #FFD700; border-radius: 50%; animation-delay: 0s;}
        .cool-bg-shape.shape2 {
            right: 8vw; top: 30vh; width: 60px; height: 60px; background: #4A4E69; border-radius: 30% 70% 70% 30%/30% 30% 70% 70%; animation-delay: 2s;}
        .cool-bg-shape.shape3 {
            left: 20vw; bottom: 10vh; width: 70px; height: 70px; background: #F2E94E; border-radius: 50% 50% 40% 60%/60% 40% 60% 40%; animation-delay: 4s;}
        .cool-bg-shape.shape4 {
            right: 18vw; bottom: 18vh; width: 100px; height: 100px; background: #22223B; border-radius: 50%; animation-delay: 6s;}
        @keyframes floatShape {
            0% { transform: translateY(0) scale(1) rotate(0deg); }
            50% { transform: translateY(-30px) scale(1.08) rotate(10deg); }
            100% { transform: translateY(0) scale(1) rotate(0deg); }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="headline"><span class="warning-icon">💥</span> The Secret to <span class="underline">Explosive</span> Social Media Growth</div>
        <div class="star-rating-row" style="display:flex;align-items:center;justify-content:center;margin-bottom:18px;gap:12px;">
            <div class="star-avatars" style="display:flex;align-items:center;">
                <img src="https://randomuser.me/api/portraits/women/1.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/men/2.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/men/3.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/women/4.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/men/5.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/women/6.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/men/7.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/women/8.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/men/9.jpg" class="star-avatar star-avatar-small">
                <img src="https://randomuser.me/api/portraits/men/10.jpg" class="star-avatar star-avatar-small">
            </div>
            <div style="display:flex;align-items:center;gap:1px;">
                <span style="font-size:1.1rem;color:#FFD700;">★</span>
                <span style="font-size:1.1rem;color:#FFD700;">★</span>
                <span style="font-size:1.1rem;color:#FFD700;">★</span>
                <span style="font-size:1.1rem;color:#FFD700;">★</span>
                <span style="font-size:1.1rem;color:#FFD700;">★</span>
            </div>
            <span style="font-size:0.95rem;color:#222;margin-left:7px;">4.9-star ratings from dozens of clients</span>
        </div>
        <div class="video-wrapper">
            <iframe src="https://www.youtube.com/embed/5wAmJMwUrxY" title="YouTube video" allowfullscreen></iframe>
        </div>
        <div class="form-section">
            <form id="leadForm" action="https://formsubmit.co/rajeshkumarrao367@gmail.com" method="POST">
                <input type="text" name="name" placeholder="Name" required>
                <input type="email" name="email" placeholder="Email" required>
                <input type="tel" name="phone" placeholder="Phone Number" required pattern="[0-9]{10,15}">
                <button type="submit" class="cta-btn" id="accessBtn">Book Now</button>
            </form>
        </div>
        <div class="highlighted-text">
            Generated Real Estate Leads for 15 Rupees. Sold flat worth 42 lakhs in a month when I was 14 years old with paid ads.
        </div>
    </div>

    <section id="how-it-works">
        <h2>How it Works</h2>
        <div style="font-size:1.15rem; font-weight:500; text-align:center; margin:18px 0 18px 0;">Fill the form and hold on your seats our team will reach out to you soon</div>
    </section>

    <section id="important-note">
        <h2>Important Note</h2>
        <p>Spots are limited. Stop Thinking This is Just Valid for 13 Minutes. Secure your team's unfair advantage today.</p>
    </section>

    <div class="testimonials" id="testimonials">
        <h2 style="text-align:center; color:var(--primary); margin-bottom:18px;">What Clients Say</h2>
        <div class="testimonial-slider" id="testimonialSlider">
            <div class="testimonial">
                <img src="https://randomuser.me/api/portraits/men/32.jpg" alt="Client 1">
                We sold a flat with a ad spent of 30000 in a 20 days<br>
                <span class="testimonial-author">— Mihir Chaudhary, Real Estate Developer</span>
            </div>
            <div class="testimonial">
                <img src="https://randomuser.me/api/portraits/men/65.jpg" alt="Client 2">
                The campaign ROI was insane. Highly recommend!<br>
                <span class="testimonial-author">— Gaurav Singh, Youtuber and Business owner</span>
            </div>
            <div class="testimonial">
                <img src="https://randomuser.me/api/portraits/men/44.jpg" alt="Client 3">
                The results are shocking !!<br>
                <span class="testimonial-author">— Nimesh Prajapati, Real Esta</span>
            </div>
        </div>
        <div class="testimonial-controls" id="testimonialControls"></div>
    </div>

    <!-- Call to Action Section at the end -->
    <section id="final-cta-section">
        <h2 class="final-cta-headline">🔥 Take Action Before It's Gone.</h2>
        <button class="final-cta-btn" onclick="document.getElementById('leadForm').scrollIntoView({behavior: 'smooth'});">Book Now</button>
        <div class="final-timer" id="final-timer">Only 13:00 minutes left</div>
    </section>

    <!-- Cool animated background shapes -->
    <div class="cool-bg-shape shape1"></div>
    <div class="cool-bg-shape shape2"></div>
    <div class="cool-bg-shape shape3"></div>
    <div class="cool-bg-shape shape4"></div>

    <script>
        // Animate sections on scroll
        function revealSections() {
            const sections = document.querySelectorAll('section');
            const trigger = window.innerHeight * 0.92;
            sections.forEach(section => {
                const top = section.getBoundingClientRect().top;
                if (top < trigger) {
                    section.classList.add('visible');
                }
            });
        }
        window.addEventListener('scroll', revealSections);
        window.addEventListener('DOMContentLoaded', revealSections);

        // Testimonial slider
        const testimonials = document.querySelectorAll('.testimonial');
        const slider = document.getElementById('testimonialSlider');
        const controls = document.getElementById('testimonialControls');
        let current = 0;
        function showTestimonial(idx) {
            slider.style.transform = `translateX(-${idx * 100}%)`;
            document.querySelectorAll('.testimonial-dot').forEach((dot, i) => {
                dot.classList.toggle('active', i === idx);
            });
        }
        testimonials.forEach((_, i) => {
            const dot = document.createElement('button');
            dot.className = 'testimonial-dot' + (i === 0 ? ' active' : '');
            dot.onclick = () => {
                current = i;
                showTestimonial(current);
            };
            controls.appendChild(dot);
        });
        function autoSlide() {
            current = (current + 1) % testimonials.length;
            showTestimonial(current);
        }
        let sliderInterval = setInterval(autoSlide, 5000);
        controls.addEventListener('mouseenter', () => clearInterval(sliderInterval));
        controls.addEventListener('mouseleave', () => sliderInterval = setInterval(autoSlide, 5000));
        showTestimonial(current);

        // 13-minute countdown timer for final CTA
        (function() {
            var timerDisplay = document.getElementById('final-timer');
            if (!timerDisplay) return;
            var totalSeconds = 13 * 60;
            function updateFinalTimer() {
                var min = Math.floor(totalSeconds / 60).toString().padStart(2, '0');
                var sec = (totalSeconds % 60).toString().padStart(2, '0');
                timerDisplay.textContent = 'Only ' + min + ':' + sec + ' minutes left';
                if (totalSeconds > 0) {
                    totalSeconds--;
                    setTimeout(updateFinalTimer, 1000);
                } else {
                    // Hide the form section when timer ends
                    var formSection = document.querySelector('.form-section');
                    if (formSection) formSection.style.display = 'none';
                    // Show the sorry message
                    var sorryMsg = document.createElement('div');
                    sorryMsg.textContent = 'Sorry, come again. You are too slow.';
                    sorryMsg.style.textAlign = 'center';
                    sorryMsg.style.fontSize = '1.5rem';
                    sorryMsg.style.color = '#e53935';
                    sorryMsg.style.fontWeight = '700';
                    sorryMsg.style.margin = '32px auto';
                    document.querySelector('.container').insertBefore(sorryMsg, document.querySelector('.highlighted-text'));
                }
            }
            updateFinalTimer();
        })();

        document.getElementById('leadForm').addEventListener('submit', function(e) {
            e.preventDefault();
            // Show thank you message and hide form
            var formSection = document.querySelector('.form-section');
            if (formSection) formSection.style.display = 'none';
            var thankYouMsg = document.createElement('div');
            thankYouMsg.textContent = 'Thank you! You are the action taker. Our team will reach out to you soon.';
            thankYouMsg.style.textAlign = 'center';
            thankYouMsg.style.fontSize = '1.5rem';
            thankYouMsg.style.color = '#388e3c';
            thankYouMsg.style.fontWeight = '700';
            thankYouMsg.style.margin = '32px auto';
            document.querySelector('.container').insertBefore(thankYouMsg, document.querySelector('.highlighted-text'));
            // Optionally, you can send the form data to a backend or service here
        });
    </script>
</body>
</html>
